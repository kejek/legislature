/**
 * Created by cla283 on 6/13/2017.
 */
// create redips container
/*var redips = {};

redips.init = function () {
    // reference to the REDIPS.drag object
    var rd = REDIPS.drag;
    // initialization
    rd.init();


    redips.divNodeList = document.getElementById('checkout-table').getElementsByTagName('div');

    // element is dropped
    rd.event.dropped = function () {
        var objOld = rd.objOld,					// original object
            targetCell = rd.td.target,			// target cell
            targetRow = targetCell.parentNode,	// target row
            i, objNew;							// local variables
            console.log(objOld);
            console.log(targetCell);
            console.log(targetRow);
            if (targetCell.id == 1){
                console.log("User is in!")
            }
    };
};

// add onload event listener
if (window.addEventListener) {
	window.addEventListener('load', redips.init, false);
}
else if (window.attachEvent) {
	window.attachEvent('onload', redips.init);
}*/